<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.attendance.mapper.TeacherMapper">
    <select id="selectByTeacherId" resultType="com.attendance.entity.Teacher">
        SELECT teacher_id AS teacherId, teacher_name AS teacherName, password, phone
        FROM teachers 
        WHERE teacher_id = #{teacherId}
    </select>
    <select id="selectByTeacherIdAndPhone" resultType="com.attendance.entity.Teacher">
        SELECT teacher_id AS teacherId, teacher_name AS teacherName, password, phone
        FROM teachers
        WHERE teacher_id = #{teacherId} AND phone = #{phone}
    </select>
</mapper>
