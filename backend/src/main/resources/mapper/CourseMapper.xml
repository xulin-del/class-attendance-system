<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.attendance.mapper.CourseMapper">
    <select id="selectByTeacherId" resultType="com.attendance.entity.Course">
        SELECT course_id AS courseId, course_name AS courseName, teacher_id AS teacherId, class_name AS className 
        FROM courses 
        WHERE teacher_id = #{teacherId}
    </select>
    <select id="selectByClassName" resultType="com.attendance.entity.Course">
        SELECT course_id AS courseId, course_name AS courseName, teacher_id AS teacherId, class_name AS className 
        FROM courses 
        WHERE class_name = #{className}
    </select>
</mapper>